import numpy as np
V = np.zeros((3,3))
policy = ['R','D','L','U']
rewards = {(1,1):2,(2,2):5,(0,1):-2}
gamma = 0.9
for _ in range(10):
    for i in range(3):
        for j in range(3):
            r = rewards.get((i,j),0)
            V[i,j] = r + gamma*np.max(V)
print(V) 
